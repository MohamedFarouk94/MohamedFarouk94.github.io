# Copy this file to secrets_config.py and put a real, long, random
# key in here. secrets_config.py is listed in .gitignore so it never
# gets committed or pushed anywhere.
#
# Generate a decent key locally with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"

ADMIN_KEY = "REPLACE_ME_WITH_A_LONG_RANDOM_STRING"
